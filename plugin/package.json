{
  "name": "opencode-remote-provider",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node dist/state.test.js",
    "verify:remote": "npm run build && node ../scripts/verify-remote-lifecycle.mjs"
  },
  "dependencies": {
    "@opencode-ai/plugin": "^1.14.39"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^5.8.3"
  },
  "oc-plugin": [
    "workspace-adaptor"
  ]
}
